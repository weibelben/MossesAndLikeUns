The docker compose file in this folder lives in cd ~/tailscale-subnet/docker-compose.yml.
This allows connection to the Nebuchadnezzar server/vLAN from any device on the tailnet.

Startup container via

```shell
docker compose up -d
```
